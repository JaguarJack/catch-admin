日期选择器
<el-form-item label="{label}" prop="{prop}" {rule}>
  <el-date-picker
      v-model="{model-value}"
      type="date"
      name="{prop}"
      placeholder="选择日期"
      value-format="YYYY-MM-DD"
      clearable
  />
</el-form-item>
